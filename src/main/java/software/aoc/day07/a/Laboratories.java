package software.aoc.day07.a;

import java.util.Arrays;
import java.util.stream.Stream;

public class Laboratories {
    public static long execute(String[] s) {
        Arrays.stream(s).forEach(l -> {
            Stream.of(l).forEach(c -> {
            });
        });
        return 1L;
    }
}
